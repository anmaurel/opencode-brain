---
description: Implement with parallel subagent orchestration
agent: build
---

Implement this request end-to-end. Decompose it into independent sub-tasks, delegate safe parallel work to subagents when useful, integrate the results, verify the final state, and report changed files plus checks run:

$ARGUMENTS

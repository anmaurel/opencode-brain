# RAG TODO

- Start Qdrant.
- Start LM Studio with an embedding model.
- Run `brain_status`.
- Run `/brain-index` or `brain_reindex`.
- Create repo docs with `/brain-sync` when starting a new project.
